#ifndef EXPERIMENT_RUNNER_H
#define EXPERIMENT_RUNNER_H

#include <vector>
#include <string>
#include "Point.h"

void runExperiments(const std::vector<Point>& allPoints, const std::vector<int>& sizes, const std::string& outputFile);

#endif
